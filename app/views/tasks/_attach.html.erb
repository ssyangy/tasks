<%= form_for([@project, @task], :html => {:class => "form-horizontal", :style=>"margin-bottom:0px;"}) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="modal-body task-form clearfix">
		<div class="control-group"><i class="icon-file"></i>
		<em>Upload attachments:</em></div>
		<div class="control-group">
			<div class="file-parking input-xxxlarge">Drag and drop files here ...</div>
			<input class="input-file" type="file" multiple>
		</div>
	</div>
	<div class="modal-footer">
	  <a href="#" class="btn btn-primary" data-dismiss="modal">Done</a>
	</div>
<% end %>
